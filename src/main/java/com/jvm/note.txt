1.char(10)和varchar(10)的区别：
1.1 char(10)的长度是固定的，varchar(10)的长度是可变的。
1.2 char(10)字符是 'abc'那么存进去的长度是10，后面填充空格，varchar的长度
就会变成3；在取数据的时候，char类型的要用trim()来去除多余的空格，而varchar不需要。
1.3 char的存取速度高，因为长度固定，方便存储和查找，但空间有损耗。
    varchar对空间使用率比较好，但存取效率有损失。
1.4 char存英文占用1个字节，存中文占用两个字节；
    varchar存英文中文都是占用两个字节。